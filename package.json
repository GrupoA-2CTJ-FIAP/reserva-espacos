{
  "name": "reserva-espacos",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npm run prestart && npx concurrently \"npm run start:backend\" \"npm run start:frontend\"",
    "prestart": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "start:backend": "npm start --prefix backend",
    "start:frontend": "npm start --prefix frontend"
  },
  "dependencies": {
    "axios": "^1.7.2"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
